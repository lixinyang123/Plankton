name: Test

on:
  push:
    branches: [ main ]

jobs:

  build-image:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Restore dependences
      run: yarn

    - name: Test
      run: yarn test
